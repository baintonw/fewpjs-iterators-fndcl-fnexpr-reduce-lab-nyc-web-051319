const batteryBatches = [4, 5, 3, 4, 4, 6, 5];

let totalBatteries = batteryBatches.reduce(function(total, element){ return element + total});
// Code your solution here                          //total argument, element

//let doubledAndSummed = [1, 2, 3].reduce(function(total, element){ return element * 2 + total})
